syntax = "proto3";


message Token{
	string token = 1;
}

enum UserType{
	UNDEFINED		= 0;
	CUSTOMER		= 1;
	SHOPKEEPER	= 2;
}

message User{
	string _id = 1;
	string name = 2;
	string email = 3;
	UserType user_type = 4;
}

service Authentication{
	rpc VerifyToken(Token) returns (User);
}
