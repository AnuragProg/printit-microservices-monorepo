import { WebSocket } from "@fastify/websocket";
import { FastifyRequest } from "fastify";


async function liveTrafficWebsocket(socket: WebSocket, req: FastifyRequest){


}


export default liveTrafficWebsocket;
